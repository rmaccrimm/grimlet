.begin main
  .arm
  cmp r0, #1
  ble base_case
  mov r1, r0
  mov r0, #1
loop:
  mul r0, r0, r1  
  subs r1, r1, #1
  bgt loop
  mov r1, r0
  b end
base_case:
  mov r1, #1
  b end
end:
  // exit when pc >= 40
  nop
.end
