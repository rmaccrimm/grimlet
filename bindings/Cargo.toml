[package]
name = "bindings"
version.workspace = true
edition.workspace = true

[lib]
name = "bindings"
crate-type = ["staticlib"]

[build-dependencies]
cbindgen = "0.29.4"

[dependencies]
core = { path = "../core" }
